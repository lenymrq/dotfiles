backend = "glx";
blur-background = true;
blur-method = "dual_kawase";
blur-strength = 10;
blur-background-exclude = [
	"class_g != 'Alacritty' && class_g != 'com.mitchellh.ghostty' && class_g != 'kitty' && class_g != 'st-256color'"
];
# corner-radius = 6;
# rounded-corners-exclude = [
# 	"window_type != 'notification'"
# ];
inactive-dim = 0.20;
