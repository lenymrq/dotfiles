backend = "glx";
# blur-background = true;
# blur-method = "dual_kawase";
# blur-strength = 6;
# blur-background-exclude = [
# 	"class_g != 'Alacritty' && class_g != 'com.mitchellh.ghostty' && class_g != 'kitty' && class_g != 'st-256color' && class_g != 'Rofi'"
# ];
# inactive-dim = 0.20;
# corner-radius = 6;
# rounded-corners-exclude = [
# 	"window_type != 'notification'"
# ];
